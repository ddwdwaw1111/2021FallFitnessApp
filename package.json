{
  "name": "2021fallFitnessApp",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch server server/index",
    "start": "node server/index"
  },
  "dependencies": {
    "express": "4.17.1",
    "mongodb": "^4.1.4",
    "bcrypt": "^5.0.1",
    "dotenv": "^10.0.0",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}